# 📝 Object Detection from Scratch: Learning the Core Logic
### 객체 탐지(Object Detection) 의 핵심 원리

![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)
![PyTorch](https://img.shields.io/badge/PyTorch-2.0+-ee4c2c.svg)
![Torchvision](https://img.shields.io/badge/Torchvision-Latest-orange.svg)
![NumPy](https://img.shields.io/badge/NumPy-1.24+-blue.svg)

---

## 📌 프로젝트 요약 (Project Overview)
이번 프로젝트에서는 "컴퓨터가 이미지 속 물체의 위치를 어떻게 찾아내는 걸까?"라는 원초적인 궁금증을 바탕으로 단순히 이미 만들어진 라이브러리를 가져다 쓰는 것에 그치지 않고, 그 내부에서 돌아가는 핵심 로직들을 하나씩 직접 구현하며 객체 탐지의 '기초 체력'을 기르는 것을 목표로 삼았습니다. 바운딩 박스를 계산하는 아주 기초적인 수학부터 시작해서, 여러 개의 박스 중 가장 정확한 것만 남기는 알고리즘, 그리고 실제로 물체를 찾아내는 신경망을 설계하는 과정까지를 차례대로 담고 있습니다.

---

### ✍️ 주요 학습 포인트
| 단계 &emsp;&emsp;&emsp;&emsp; | 주요 내용 | 한 마디로? |
| :--- | :--- | :--- |
| **1. 기초 연산** | 두 박스가 얼마나 겹치는지 계산하는 IoU(Intersection over Union) 로직 구현 | "얼마나 정확하게 겹쳤나?" |
| **2. 결과 정제** | 중복된 예측 박스들을 하나로 정리하는 NMS(Non-Maximum Suppression) 알고리즘 구현 | "지저분한 박스들 정리하기" |
| **3. 모델 설계** | 이미지 특징을 추출하고 위치(좌표)와 종류(클래스)를 동시에 맞추는 신경망 구성 | "진짜 탐지기 만들기" |

---

## 🎯 핵심 목표 (Motivation)
| 핵심 질문 (Question) | 해결하기 위한 노력 (Approach) | 깨달은 점 (Insight) &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp; |
| :--- | :--- | :--- |
| **"박스가 겹친 정도를 어떻게 수치화할까?"** | IoU(Intersection over Union) 계산식을 직접 함수로 구현 | 컴퓨터가 이미지 좌표를 인식하고 면적을 계산하는 기초 기하학 원리 이해 |
| **"지저분하게 겹친 박스들을 어떻게 치울까?"** | NMS(Non-Maximum Suppression) 알고리즘 로직 설계 | 수많은 예측 중 가장 높은 확률만 남기는 효율적인 후처리 데이터 필터링 방식 체득 |
| **"하나의 신경망이 두 가지 일을 할 수 있을까?"** | 위치(Regression)와 분류(Classification)를 동시에 수행하는 <br>모델 구조 설계 | 서로 다른 성격의 정답(좌표와 이름)을 함께 학습시키는 멀티태스크 학습의 기초 확립 |

---

## 📂 프로젝트 구조 (Project Structure)
```text
├── src/
│   ├── engine/
│   │   └── ops.py           # IoU, NMS 등 핵심 수학 연산 모듈
│   └── models/
│       └── detector.py      # 탐지 모델 아키텍처 및 손실 함수 정의
├── .gitignore               # 불필요한 파일 업로드 방지
├── LICENSE                  # MIT License (AD-Styles)
├── README.md                # 프로젝트 리포트
└── requirements.txt         # 라이브러리 설치 목록
```

---

## 🛠️ 핵심 구현 기술 (Technical Implementation)
| 핵심 개념 (Core Concept) | 구현한 방식 (Implementation Details) | 깨달은 점 (Key Insight) |
| :--- | :--- | :--- |
| **좌표 포맷 변환 유틸리티** | `[cx, cy, w, h]` → `[x1, y1, x2, y2]` 변환하는 `xywh_to_xyxy` 함수 구현. 모델 출력(Sigmoid 정규화 좌표)과 IoU 연산 입력 포맷 간의 호환성을 맞추기 위한 필수 전처리 단계. | 모델이 출력하는 좌표 표현 방식과 실제 계산에 쓰이는 포맷이 다름을 이해하고, 파이프라인의 각 단계를 명확히 분리하는 설계 역량 습득. |
| **IoU 계산 <br>알고리즘** | 교집합/합집합 계산 로직의 `calculate_iou` 함수 직접 구현. 분모 0 에러 방지를 위한 `1e-16`(Epsilon) 처리 코드 추가. | 컴퓨터가 픽셀 좌표를 이용해 면적과 겹침 정도를 계산하는 기하학적 원리의 근본적 이해. |
| **NMS 후처리 <br>로직** | 중복 박스 제거를 위한 `nms` 함수 설계 및 구현. 신뢰도(Confidence Score) 기반의 데이터 필터링 로직 적용. `cv2.dnn.NMSBoxes` 의존 없이 순수 NumPy로 재현. | 지저분한 예측 결과물을 하나로 정리하는 데이터 후처리 프로세스의 중요성 및 실무적 효용 체감. |
| **Multi-task <br>신경망** | ResNet18 Backbone 기반의 좌표(Regression) 및 분류(Classification) Head 분리 설계. `alpha`, `beta` 가중치를 활용한 Loss 밸런싱 구조 최적화. Regression 출력에 `Sigmoid` 적용으로 좌표를 0~1로 정규화하여 이미지 해상도 독립적인 예측 구현. | 서로 다른 성격의 학습 목표들 사이에서 균형을 잡는 유연한 모델 설계 역량 습득. |
| **학습 안정화 <br>세팅** | `He Initialization` 가중치 초기화 및 `BatchNorm1d` 적용을 통한 학습 초기 수렴 속도 개선. | 이론으로 배운 최적화 기법들이 실제 모델의 학습 안정화와 성능에 미치는 결정적 영향 확인. |

---

## 📊 학습 성과 및 지표 (Results)

> **좌표 정규화 방식 안내:** 본 모델의 Regression Head는 `Sigmoid`를 적용하여 bbox 좌표를 0~1 범위로 정규화하여 출력합니다. 따라서 학습 시 정답 bbox도 동일하게 이미지 크기로 나누어 정규화한 뒤 사용하였으며, MSE Loss 값의 절대 스케일은 픽셀 단위로 학습한 경우(수백~수천)와 달리 0~1 범위 내에서 계산됩니다.

| 항목 (Metrics) | 학습 초기 (Epoch 1) | 학습 완료 (Epoch 100) | 결과에 대한 해석 (Interpretation) |
| :--- | :---: | :---: | :--- |
| **위치 오차 (BBox MSE Loss)** | 0.31 | **0.004** | 정규화 좌표(0~1) 기준으로 초기에는 박스가 엉뚱한 곳에 그려졌으나, 학습 후 실제 물체 위치에 근접하게 수렴 |
| **분류 오차 (Class CE Loss)** | 1.65 | **0.02** | 초반엔 물체 이름을 헷갈려 했지만, 나중에는 거의 100%에 가까운 확률로 이름을 맞춤 |
| **전체 오차 (Total Loss)** | 1.96 | **0.024** | 두 가지 목표(위치+이름)가 서로 충돌하지 않고 안정적으로 줄어들며 학습이 수렴 |

**100번의 반복 학습(Epochs)을 거치며 모델이 어떻게 정답을 찾아가는지 관찰했습니다. 초기에는 박스 위치를 전혀 잡지 못했지만, 학습이 진행될수록 위치와 종류를 모두 정확하게 맞추기 시작했습니다.**

### 💡 주요 성과 (Key Achievement)

- **손실 함수(Loss) 98% 이상 감소**: 초기 오차에 비해 최종 오차를 획기적으로 줄이며 모델의 예측 정밀도를 확보했습니다.
- **안정적인 수렴**: 학습 정체 구간이 있었으나, 배치 정규화(Batch Normalization)를 적용하여 흔들림 없이 학습을 마무리할 수 있었습니다.

---

## 💡 회고록 (Retrospective)

&emsp;&emsp;이번 프로젝트를 통해 AI 모델 개발에서 "이론적 기반"이 실무 엔지니어링에 얼마나 큰 영향을 미치는지 재확인했습니다. 직접 IoU 수식을 짜보고, NMS 로직을 설계하며, 두 갈래의 신경망을 연결해 보는 과정을 통해 **"당연하게 돌아가는 것은 없다"** 는 소중한 교훈을 얻었습니다. 픽셀 좌표 하나하나를 계산하며 분모가 0이 되어 프로그램이 멈추는 작은 에러를 잡았을 때, 비로소 '엔지니어링의 디테일'이 무엇인지 조금이나마 깨달을 수 있었습니다. 또한 위치와 종류라는 서로 다른 두 가지 정답을 동시에 맞추기 위해 신경망의 균형을 잡는 과정은 딥러닝 아키텍처의 기본을 느끼게 해 준 값진 경험이었습니다. 특히, 단순히 모델을 돌려보는 것에 그치지 않고 지저분한 연습장 같던 코드를 파일별로 모듈화하여 정리하며, '돌아가는 코드'를 넘어 '관리할 수 있는 코드'의 가치도 배울 수 있었습니다. 처음으로 내가 짠 로직을 거친 빨간 박스가 이미지 속 물체를 정확히 찾아냈을 때의 소중한 경험은, 앞으로 제가 AI 엔지니어로 성장해 나가는 데 있어 가장 강력한 동기부여가 될 것입니다. 
<br>&emsp;&emsp;여기서 멈추지 않고, 이제는 '기초'를 넘어 '실전'으로 나아가려 합니다. 본 프로젝트에서 구현한 로직들이 실제 SOTA(State-of-the-Art) 모델인 YOLOv5에서는 어떻게 최적화되어 작동하는지, 그리고 실시간성(Real-time)이라는 실무적 제약을 어떻게 극복하는지 탐구하는 것이 저의 다음 목표입니다. 이어지는 프로젝트 yolov5-pothole-detector에서는 이번에 배운 이론적 토대 위에 '실무 서비스 적용'과 '성능 최적화'라는 경험을 쌓아, 원리와 실천을 겸비한 엔지니어로 성장해 나가겠습니다.
